{
  "name": "agent-estate",
  "version": "0.1.0",
  "private": true,
  "description": "VS Code-first AI agent and MCP governance tooling for local audit reports.",
  "license": "UNLICENSED",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm --workspace @agent-estate/vscode-extension run build",
    "check": "node scripts/validate-project.mjs",
    "fixture:report": "node scripts/generate-fixture-report.mjs",
    "discovery:report": "node scripts/generate-readonly-report.mjs",
    "report:markdown": "node scripts/render-markdown-report.mjs",
    "smoke": "node scripts/validate-project.mjs --smoke && npm run build && npm run fixture:report && npm run report:markdown -- --check && npm run discovery:report -- --check"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {}
}
